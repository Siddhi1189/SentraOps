import { OverviewView } from '../features/dashboard/components/OverviewView';

export function OverviewPage() {
  return (
    <main style={{ padding: 'var(--space-6)', maxWidth: '1200px', margin: '0 auto' }}>
      <OverviewView />
    </main>
  );
}
